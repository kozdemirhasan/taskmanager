# Server ayarları
server.port=${PORT:8080}
spring.application.name=football-team-application

# PostgreSQL veritabanı ayarları
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${JDBC_DATABASE_USERNAME}
spring.datasource.password=${JDBC_DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# MySQL veritabani ayarlari - local
#spring.datasource.url=jdbc:mysql://localhost:3306/football_team_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# PostgreSQL bağlantı ayarları
#spring.datasource.url=jdbc:postgresql://localhost:5432/football_team_db?ssl=false
#spring.datasource.username=postgres
#spring.datasource.password=12345
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA ayarları
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Hibernate'in tablo oluşturma stratejisini belirle
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Dosya yükleme ayarları
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Mesaj kaynaklarını yapılandır
spring.messages.basename=messages
spring.messages.encoding=UTF-8
spring.messages.fallback-to-system-locale=false
spring.messages.use-code-as-default-message=true